# TransitionResult Contract

Initial contract placeholder for `TRANS-001`.

`transition(full_state, joint_action, rng)` must return the next authoritative state, emitted raw/derived events, branch identity, parent identity, simulator version, and explicit RNG/seed lineage. Approximate evaluators must not be represented as exact transitions.

